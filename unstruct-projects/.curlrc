# -u = "username:password"
user = "<username:password>"

# -s = be silent; don't show download statistics
silent

# -S = show error messages generated
show-error

# -k = don't check certs (not advised)
